{% extends 'baseadmin.html.twig' %}

{% block title %}User1 index{% endblock %}

{% block body %}
    <h1>User1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user1 in user1s %}
            <tr>
                <td>{{ user1.id }}</td>
                <td>{{ user1.email }}</td>
                <td>{{ user1.roles ? user1.roles|json_encode : '' }}</td>
                <td>{{ user1.password }}</td>
                <td>
                    <a href="{{ path('app_user1_show', {'id': user1.id}) }}">show</a>
                    <a href="{{ path('app_user1_edit', {'id': user1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_user1_new') }}">Create new</a>
{% endblock %}
